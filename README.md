# Datadriver-Templates
Some templates for DataDriver.
Place these in `config/datadriver/vanilla/templates`.
(You'll probably need to make the `vanilla` folder.)
Also place the `package.json` in said `vanilla` folder.
